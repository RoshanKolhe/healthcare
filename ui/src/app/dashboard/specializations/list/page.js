// sections
import SpecializationListView from 'src/sections/specialization/view/specialization-list-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new specialization',
};

export default function SpecializationListPage() {
  return <SpecializationListView />;
}
