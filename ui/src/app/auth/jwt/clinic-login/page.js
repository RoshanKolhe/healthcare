// sections
import { JwtClinicLoginView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Jwt: Clinic Login',
};

export default function ClinicLoginPage() {
  return <JwtClinicLoginView />;
}
